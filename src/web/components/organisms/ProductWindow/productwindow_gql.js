import React from 'react'

import { compose } from 'async';
import {
    CART_ADD_ITEM,
    CART_SUBTRACT_ITEM,
    CART_REMOVE_ITEM
} from '../../logic/cart/cart_mutation';
import { CART_GQL_QUERY } from '../../logic/cart/cart_query';

const ProductWindowGqlComponent = (props) => {
    return (
        <div>

        </div>
    )
}
export default compose()(ProductWindowGqlComponent)